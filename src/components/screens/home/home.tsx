import React, {FC} from "react";
import {Text} from "react-native";
import {Header, Layout} from "@layouts/index";

export const Home: FC = () => {
	return (
		<Layout>
			<Header/>
			<Text></Text>
		</Layout>
	);
}
